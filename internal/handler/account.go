package handler

// account.go exists to satisfy the task requirement for an account handler file.
// Me and related user mutations are already provided in auth.go; this file
// intentionally keeps no additional handlers to avoid duplication.
